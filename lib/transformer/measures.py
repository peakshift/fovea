def gpr(gross_profit, net_sales):
    """Calculate the Gross Profit Ratio.

    Args:
        gross_profit (int): The gross profit.
        net_sales (int): The net sales.

    Returns:
        float: Returns a float if successul.
        bool: False if not successful.

    """
    return False

def opcr(op_cost, net_sales):
    """Calculate the Operating Cost Ratio.

    Args:
        op_cost (int): The operating cost.
        net_sales (int): The net sales.

    Returns:
        float: Returns a float if successul.
        bool: False if not successful.

    """
    return False

def oppr(op_profit, net_sales):
    """Calculate the Operating Profit Ratio, also called the Net Profit Ratio.

    Args:
        op_profit (int): The operating profit.
        net_sales (int): The net sales.

    Returns:
        float: Returns a float if successul.
        bool: False if not successful.

    """
    return False

def opr(net_profit, tl_assets):
    """Calculate the Overal Profitability Ratio.

    Args:
        net_profit (int): The net profit.
        tl_assets (int): The total assets.

    Returns:
        float: Returns a float if successul.
        bool: False if not successful.

    """
    return False
